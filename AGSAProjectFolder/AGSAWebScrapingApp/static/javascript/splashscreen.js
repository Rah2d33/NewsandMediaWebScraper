document.addEventListener("DOMContentLoaded", () => {
    setTimeout(() => {
        // Redirect to the profile search page (ensure the URL is correct)
        window.location.href = "/index";  // Replace '/index' with the correct URL if needed
    }, 5000);  // Delay for 3 seconds before redirection
});